# Coding Guidelines

* Prefer imports over using fully qualified names. When referencing symbols from other modules, import them explicitly rather than using long qualified paths. This keeps the code concise and easier to read.

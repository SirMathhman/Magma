# Magma Java Project

This is a minimal Java project scaffolded for the repository. It uses Maven and includes a small sample utility method and a unit test.

Quick start:

PowerShell:

```
.\check-java.ps1
mvn -q test
```

Note: This environment already has OpenJDK 24 installed.

<?xml version="1.0"?>
<!DOCTYPE module PUBLIC "-//Puppy Crawl//DTD Check Configuration 1.3//EN" "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
  <!-- Apply suppressions (see suppressions.xml) -->
  <module name="SuppressionFilter">
    <property name="file" value="config/checkstyle/suppressions.xml" />
  </module>
  <!-- Forbid textual mentions of java.util.regex anywhere in the source -->
  <module name="RegexpSingleline">
    <property name="format" value="java\.util\.regex" />
    <property name="message" value="Usage of java.util.regex is forbidden; prefer dedicated parsing helpers instead" />
  </module>
  <!-- Only enforce maximum methods per class -->
  <module name="TreeWalker">
    <module name="MethodCount">
      <property name="maxTotal" value="20" />
      <!-- include constructors in the count? default is true; keep default -->
    </module>

    <!-- Limit number of parameters per method to encourage small helpers and parameter objects -->
    <module name="ParameterNumber">
      <property name="max" value="3" />
    </module>

    <!-- Keep all illegal-token checks except void here -->
    <module name="IllegalToken">
      <property name="tokens"
        value="LITERAL_NULL,LITERAL_THROW,LITERAL_CONTINUE,LITERAL_BREAK,TYPECAST" />
    </module>

    <!-- Isolated rule for 'void' so it can be suppressed selectively via suppressions.xml -->
    <module name="IllegalToken">
      <property name="tokens" value="LITERAL_VOID" />
    </module>

    <module name="IllegalType">
      <property name="illegalClassNames" value="Object,java.lang.Object" />
    </module>
    <!-- Forbid use of java.util.regex package imports and references -->
    <module name="IllegalImport">
      <property name="illegalPkgs" value="java.util.regex" />
    </module>
  </module>
</module>
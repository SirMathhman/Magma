{
  "name": "magma-ts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
  "test": "jest --colors --config jest.config.cjs",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "jest": "^29.6.1",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  }
}

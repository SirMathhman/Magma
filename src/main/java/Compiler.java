public class Compiler {
  /**
   * Compile the given source string. Currently returns an empty string.
   * 
   * @param input source to compile (not used)
   * @return empty string
   */
  public static String compile(String input) {
    return "";
  }
}

package magma.compiler;

import magma.compiler.ast.Program;

public class SemanticAnalyzer {
	public static void analyze(Program program) {
		// placeholder for semantic checks (types, undefined names, etc.)
		// currently does nothing
	}
}

package org.example.core;

/**
 * Dummy placeholder to avoid duplicate type errors during migration. This file
 * is
 * intentionally minimal. The real Option sealed interface lives in
 * `Option.java`.
 */
public final class None {
	private None() {
	}
}

struct FirstLocator() implements Locator{};
Option<Integer> locate_FirstLocator() implements Locator(char* input, char* infix) {/*
		final int index = input.indexOf(infix); return index == -1 ? Option.empty() : Option.of(index);
	*/}

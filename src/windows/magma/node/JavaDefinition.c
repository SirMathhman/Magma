#include "JavaDefinition.h"
/*

public record JavaDefinition(String beforeType, JavaType type, String name) {
    public CDefinition toCDefinition() {
        return new CDefinition(this.beforeType(),
                this.type().toCType(), this.name());
    }
}*//**/
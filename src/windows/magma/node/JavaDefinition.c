#include "JavaDefinition.h"
/*

public record JavaDefinition(String beforeType, JavaType type, String name) implements JavaHeader {
    public CHeader toCDefinition() {
        return this.toCDefinition("");
    }

    public CHeader toCDefinition(final String suffix) {
        return new CDefinition(this.beforeType, this.type.toCType(), this.name + suffix);
    }
}*//**/